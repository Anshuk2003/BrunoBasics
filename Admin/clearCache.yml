info:
  name: clearCache
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/admin/clear-app-cache"
  auth:
    type: basic
    username: Anshuk
    password: "{{adminPassword}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

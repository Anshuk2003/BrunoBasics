info:
  name: getAllUsers
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/admin/all-users"
  auth:
    type: basic
    username: Anshuk
    password: Abhay

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

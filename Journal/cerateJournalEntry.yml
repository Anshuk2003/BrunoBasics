info:
  name: cerateJournalEntry
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/journal/"
  body:
    type: json
    data: |-
      {
          "title":"Bruno Basics",
          "content":"Learning Bruno Basics"
      }
  auth:
    type: basic
    username: Bruno
    password: Bruno

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

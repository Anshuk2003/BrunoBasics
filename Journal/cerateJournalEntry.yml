info:
  name: cerateJournalEntry
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/journal/"
  auth:
    type: basic
    username: Bruno
    password: Bruno

runtime:
  scripts:
    - type: before-request
      code: |-
        const body={
          "title":"Bruno Journal Entry",
          "content": "Adding Bruno's first journal Entry"
        };

        req.setHeader("Content-Type", "application/json");
        req.setBody(body);
        console.log(req.getName())

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: deleteUser
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseURL}}/user"
  auth:
    type: basic
    username: ABCD
    password: "{{userPassword}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

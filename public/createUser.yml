info:
  name: createUser
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/public/create-user"
  body:
    type: json
    data: |-
      {
          "username":"BrunoVs",
          "password":"BrunoVs"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

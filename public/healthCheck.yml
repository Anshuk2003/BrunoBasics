info:
  name: healthCheck
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/public/health-check"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

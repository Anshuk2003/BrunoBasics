info:
  name: healthCheck
  type: http
  seq: 1

http:
  method: GET
  url: "{{process.env.BASE_URL}}/public/health-check"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
